// Realiza um tarefa, não devolve nada

function dizerNome() {
    console.log('jhonatan');
}

dizerNome();

//Faz um cálculo ou operação e retornar algo
function MultiplicarPorDois(valor) {
    return valor * 2;
}

// console.log(MultiplicarPorDois(5)); //resultado é 10

//aplicar dentro de uma variavel

let resultado = MultiplicarPorDois(5);
console.log(resultado);
